{
  "devDependencies": {
    "tailwindcss": "^3.3.2"
  },
  "scripts": {
    "tailwind": "npx tailwindcss -i ./style/style.css -o ./dist/output.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
